using System;

namespace EFCosmosDB.Extensions
{
    public static class RefecltionExtensions
    {
        //public static T CreateInstance<T>(this Activator activator, params object[] paramArray)
        //{
        //    return (T)Activator.CreateInstance(typeof(T), args: paramArray);
        //}
    }
}
